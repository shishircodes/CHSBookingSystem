# Set this in the Vercel project settings (Environment Variables).
# Use the full URL of the deployed backend, including https:// but no trailing slash.
VITE_API_URL=https://your-backend.vercel.app/api
